function pokemonBattle(pokemon1, pokemon2) {
  if (pokemon1 === 'magikarp' || pokemon2 === 'magikarp') {
    return 'magikarp';
  } else {
    return 'pikachu';
  }
}

// Falta corrigir semi, que está ligado à opção Unix/Windows

